# Sample Data

This directory contains dummy/synthetic data for testing.

## Real data

Place your real data (WhisperX JSON output, SRT files, etc.) in `poc/data/` (gitignored).

## Format reference

See `dummy_whisperx.json` for the expected WhisperX output format.
